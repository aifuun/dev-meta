# Samples

这里放基于 `docs/02-version-rules.md` 生成的版本样例。

## 当前样例

- `V1.4.1-indexeddb-prefs/` — IndexedDB 基础通道与 per-trackId 播放偏好持久化样例
- `contract-gate/` — 契约断言门禁参考实现（SHA256 MANIFEST + `contract_verified` + JSON Schema 校验），对应 `docs/06` §2.8 与 `skills/dm-contract-gate.md`
