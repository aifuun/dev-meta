# {{PROJECT_NAME}} - 系统设计

> **文档流向纪律**：本文档继承引用 upstream `00_PRODUCT_REQUIREMENTS.md`、`01_TECHNICAL_SPEC.md`，并被 `03`、`04`、`05`、`06` 引用；**不反向引用下游**。
> **占位符约定**：`{{FIELD}}` = 结构化命名字段；`<!-- TODO: [dm-init-docs] <说明> -->` = 待补充内容。
> **权威引用**：可观测性规范见 `dev-meta/docs/07`（唯一权威），本文**只放指针**，详细实例化在 `06_OBSERVABILITY.md`；架构设计原则见 `dev-meta/docs/09`。

---

## 1. 总体架构

```
<!-- TODO: [dm-init-docs] 总体架构图（分层 / 模块 / 外部依赖） -->
```

**分层原则**：<!-- TODO: [dm-init-docs] 依赖方向（如单向分层：上层可依赖下层，下层不得反向依赖） -->

| 层 | 职责 | 可依赖 | 禁止 |
|----|------|--------|------|
| <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> |

---

## 2. 组件与模块划分

| 模块 | 职责 | 对外暴露（Facade / API） | 内部实现 |
|------|------|--------------------------|----------|
| <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | 私有 |

> **极简暴露**：每模块仅经统一 Facade / 入口文件对外暴露，内部实现全部私有（见 `dev-meta/docs/09` §2）。

---

## 3. 数据流 / 调用链

<!-- TODO: [dm-init-docs] 核心链路：输入 → 处理 → 输出，标注跨模块边界 -->

| 步骤 | 发起方 | 接收方 | 数据 | 通信方式 |
|------|--------|--------|------|----------|
| <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | 同步 / 事件 |

> **跨模块解耦**：同步用 Mediator / RPC 代理，异步用事件总线；禁止模块间硬编码互相引用（`dev-meta/docs/09` §3.2）。

---

## 4. 并发与状态机模型

| 状态 / 阶段 | 触发 | 下一状态 | 失败态 |
|-------------|------|----------|--------|
| <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> |

- **并发模型**：<!-- TODO: [dm-init-docs] 线程 / 队列 / 异步模型，及共享资源保护方式 -->
- **状态切换可观测**：关键状态切换须留痕（详见 `06_OBSERVABILITY.md`）。

---

## 5. 可观测性设计（仅指针）

> **不在此展开**：可观测性原则、黑匣子结构、包装器的唯一权威是 `dev-meta/docs/07`；本项目的实例化（三层次模型、埋点清单、判据）见 **`06_OBSERVABILITY.md`**。
> 本节只登记「哪些关键路径必须被观测」：

| 关键路径 | 是否高开销 | 须含指标 | 落点 |
|----------|-----------|----------|------|
| <!-- TODO --> | 是 / 否 | Elapsed / 资源 / 入参快照 | `06_OBSERVABILITY.md` |

---

## 6. 引用声明

| 引用对象 | 方向 | 用途 |
|----------|------|------|
| `00_PRODUCT_REQUIREMENTS.md` | upstream | 业务意图 |
| `01_TECHNICAL_SPEC.md` | upstream | 技术选型与约束 |
| `dev-meta/docs/07` | 外部权威 | 可观测性规范（只引用） |
| `dev-meta/docs/09` | 外部权威 | 架构设计原则（只引用） |
